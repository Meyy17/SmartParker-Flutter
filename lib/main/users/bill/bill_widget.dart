import 'package:flutter/material.dart';

Widget notPaidContent() {
  return ListView.builder(
    itemCount: 1,
    itemBuilder: (BuildContext context, int index) {
      return;
    },
  );
}

Widget paidOffContent() {
  return ListView.builder(
    itemCount: 1,
    itemBuilder: (BuildContext context, int index) {
      return;
    },
  );
}
